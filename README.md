Phone Number Lookup
Overview
A simple Python script to check who owns a phone number from a predefined list.

How It Works
User enters a phone number.

The script looks it up in a dictionary.

If found → prints the owner’s name.

If not → prints "Number not found".

Requirements
Python 3.x
